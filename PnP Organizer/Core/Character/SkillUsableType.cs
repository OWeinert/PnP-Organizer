namespace PnP_Organizer.Core.Character
{
    // Defines if a skill is usable after it is skilled (SkillPoints > 0) or if it is maxed (SkillPoints = MaxSkillPoints)
    public enum SkillUsableType
    {
        Skilled,
        Maxed
    }
}
